app:
  name: "go-tutorial-phase3"
  version: "1.0.0"
  port: 8080
  mode: "debug"  # debug / release

jwt:
  secret: "your-secret-key-change-in-production"
  expire_hours: 24

log:
  level: "info"  # debug / info / warn / error
  format: "json" # json / console
